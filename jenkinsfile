pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git branch: 'main', url: 'https://github.com/Ooladipupo/jenkins_file_folder.git'
            }
        }
    }
}
